import{_ as t,o as a,c as e,a as i}from"./app.e53850ea.js";const r="/Portfolio/assets/ray-tracing-schema.5bce520a.png",w=JSON.parse('{"title":"What is Raytracing","description":"","frontmatter":{},"headers":[],"relativePath":"Software Computer Graphics/Raytracing/introduction.md"}'),s={name:"Software Computer Graphics/Raytracing/introduction.md"},o=i('<h1 id="what-is-raytracing" tabindex="-1">What is Raytracing <a class="header-anchor" href="#what-is-raytracing" aria-hidden="true">#</a></h1><p>These days most people know of the term ray-tracing but don&#39;t actually know what it does and how it works.</p><div class="tip custom-block"><p class="custom-block-title">INFO</p><p>Ray tracing is just an algorithm. That is all it is in the end.</p></div><p>Before I start to explain what ray tracing does let me first explain what it is and why it works.</p><p><img src="'+r+'" alt="schema"></p><p>In a ray-tracing application we have a camera that will shoot rays through every pixel on our screen, in doing so we will covert these camera -&gt; screen/raster rays to view space rays, when a ray misses we render nothing and when we hit something we sample the material of the object and render that 1 pixel.</p><div class="danger custom-block"><p class="custom-block-title">REMARK</p><p>This is why ray-tracing is computationally expensive to execute, for every pixel it has to shoot a ray through and see if it hit something in out scene.</p><p>On top of that we have to check if it collided or not and if it does get the right color value etc...</p><p>There are ways to excelerate this process but typically ray-tracing always takes more time than Rasterizing.</p><p>But if that is the case, why do we want to use ray-tracing at all?</p></div><p>At some point you will notice that ray-tracing is more straight forward to do and is also more realistic as with ray-tracing we essentially simulate real light, so rendering shadows and reflections will always look better in a ray-tracer.</p><p>These days we luckily have graphic cards (RTX series) that speed up ray-tracing a lot, mainly because of the neural nework inside the card.</p><p>Most game engines these days use a combination of ratserizing and ray-tracing, as a fully raytraced game with be almost unplayable.</p>',10),n=[o];function c(l,h,p,d,y,g){return a(),e("div",null,n)}const u=t(s,[["render",c]]);export{w as __pageData,u as default};
